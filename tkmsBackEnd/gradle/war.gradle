apply plugin: "war"

bootWar {
    mainClass = "com.daitoj.tkms.TkmsBackEndApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
